{
  "_comment": "OPTIONAL — not required for the core routing architecture. The routing rules are loaded automatically via the CLAUDE.md @import added by install.sh. Add this to the SessionStart array in ~/.claude/settings.json only if you want a per-session availability reminder.",
  "hooks": [
    {
      "type": "command",
      "command": "command -v claude-deepseek >/dev/null 2>&1 && echo '[claude-deepseek] DeepSeek delegation ready'"
    }
  ]
}
